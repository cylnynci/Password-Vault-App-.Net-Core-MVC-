@model EntityLayer.Concrete.Site
@{
    //ViewBag.Title = "GetSite";
    //Layout = "~/Views/Shared/_UILayout.cshtml";
}

@*@using (Html.BeginForm("UpdateAccount", "Home", FormMethod.Post))
    {
        <div class="form-group">
            @Html.LabelFor(m => m.AccountId)
            @Html.TextBoxFor(m => m.AccountId, new { @class = "form-control" })
            <br />
            @Html.LabelFor(m => m.SiteName)
            @Html.TextBoxFor(m => m.SiteName, new { @class = "form-control" })
            <br />
            @Html.LabelFor(m => m.SiteUserName)
            @Html.TextBoxFor(m => m.SiteUserName, new { @class = "form-control" })
            <br />
            @Html.LabelFor(m => m.SitePassword)
            @Html.TextBoxFor(m => m.SitePassword, new { @class = "form-control" })
        </div>
        <div>
            <button class="btn btn-warning">Update</button>
        </div>
    }*@
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</head>
<body>

    <div class="container">
        <h2>Update Site</h2><br />
        @using (Html.BeginForm("UpdateSite", "Site", FormMethod.Post))
        {
            <div class="form-group">
                @Html.LabelFor(m => m.SiteId)
                @Html.TextBoxFor(m => m.SiteId, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.SiteName)
                @Html.TextBoxFor(m => m.SiteName, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.SiteAddress)
                @Html.TextBoxFor(m => m.SiteAddress, new { @class = "form-control" })
            </div>
            <button type="submit" class="btn btn-default" onclick="window.location.href = " @Url.Action("SiteList", "Site") ";">Update</button>
        }
    </div>

</body>
</html>
